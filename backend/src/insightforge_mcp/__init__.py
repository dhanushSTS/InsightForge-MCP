"""InsightForge MCP — a Model Context Protocol server over PostgreSQL.

Exposes schema-discovery, safe read-only querying, and reporting tools so an
LLM can act as an autonomous business analyst against a relational database.
"""

__version__ = "0.1.0"
